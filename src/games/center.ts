import type { Coordinates } from "./types";

/** Centro de Campinas, usado quando a localização não está disponível. */
export const FALLBACK_CENTER: Coordinates = {
    latitude: -22.9056,
    longitude: -47.0608,
};
